# {{ module_name }}

Enterprise-grade hub-and-spoke network architecture for Azure, generated by TF Wizard.

## Overview

This Terraform configuration creates a complete enterprise infrastructure with:

- **Hub Network**: Centralized networking with Azure Firewall, VPN Gateway, and Azure Bastion
- **Spoke Networks**: {{ spoke_count }} application-specific networks with VNET peering
- **AKS Clusters**: {{ aks_clusters }} managed Kubernetes clusters with advanced security
- **Security**: Azure AD integration, custom RBAC roles, and comprehensive monitoring
- **Storage**: Secure blob storage with private endpoints
- **Identity**: Azure AD groups and custom role definitions

## Architecture Components

### Hub Infrastructure
- Virtual Network with centralized routing
- Azure Firewall for network security
- VPN Gateway for hybrid connectivity
- Azure Bastion for secure VM access
- Route tables for traffic control

### Spoke Infrastructure
{% for spoke in enterprise.spokes %}
- **{{ spoke.name }}**: {{ spoke.resource_group_name | default('rg-spoke-' + spoke.name) }}
  - Virtual Network: {{ spoke.vnet_name | default('vnet-spoke-' + spoke.name) }}
  {% if spoke.enable_aks %}- AKS Cluster with {{ spoke.aks_node_count }} nodes{% endif %}
  {% if spoke.enable_app_gateway %}- Application Gateway with WAF{% endif %}
  {% if spoke.enable_storage %}- Storage Account with private endpoints{% endif %}
  {% if spoke.enable_key_vault %}- Key Vault for secrets management{% endif %}
  {% if spoke.enable_acr %}- Container Registry for images{% endif %}
{% endfor %}

### Security & Identity
- Azure AD Groups: Platform Admins, Developers, DevOps Engineers, Security Auditors, End Users
- Custom RBAC Roles: Platform Administrator, Developer, DevOps Engineer, Security Auditor
- Network Security: NSGs, Azure Firewall, Private Endpoints
- Monitoring: Azure Monitor, Log Analytics, Security Center

## Prerequisites

1. **Azure Subscription** with sufficient permissions
2. **Azure CLI** installed and authenticated
3. **Terraform** v1.0+ installed
4. **Azure AD** permissions for group and role management

## Quick Start

```bash
# Initialize Terraform
terraform init

# Review the plan
terraform plan

# Deploy the infrastructure
terraform apply
```

## Configuration

### Hub Configuration
- Location: {{ enterprise.location }}
- Address Space: {{ enterprise.hub.address_space | join(', ') }}
- Firewall: {{ 'Enabled' if enterprise.hub.enable_firewall else 'Disabled' }}
- VPN Gateway: {{ 'Enabled' if enterprise.hub.enable_vpn_gateway else 'Disabled' }}
- Bastion: {{ 'Enabled' if enterprise.hub.enable_bastion else 'Disabled' }}

### Spoke Configuration
{% for spoke in enterprise.spokes %}
**{{ spoke.name }}**:
- Address Space: {{ spoke.address_space | join(', ') }}
- AKS: {{ 'Enabled' if spoke.enable_aks else 'Disabled' }}
- Storage: {{ 'Enabled' if spoke.enable_storage else 'Disabled' }}
- Key Vault: {{ 'Enabled' if spoke.enable_key_vault else 'Disabled' }}
- ACR: {{ 'Enabled' if spoke.enable_acr else 'Disabled' }}
{% endfor %}

## Generated Documentation

After deployment, the following documentation is automatically generated:

- **Architecture Diagram** (`architecture_diagram.md`): Mermaid diagram of the infrastructure
- **Infrastructure Documentation** (`infrastructure_documentation.md`): Detailed component descriptions
- **Deployment Guide** (`deployment_guide.md`): Step-by-step deployment instructions
- **Security Overview** (`security_overview.md`): Security measures and compliance
- **Cost Estimation** (`cost_estimation.md`): Monthly cost breakdown

## Security Features

- **Network Isolation**: Hub-and-spoke topology with forced tunneling
- **Identity Management**: Azure AD integration with conditional access
- **Encryption**: Data at rest and in transit
- **Monitoring**: Comprehensive logging and alerting
- **Compliance**: Built-in security controls and policies

## Cost Optimization

- Reserved instances for long-term deployments
- Autoscaling for AKS clusters
- Lifecycle policies for storage
- Monitoring cost controls

## Support

For issues or questions:
- Check the generated documentation
- Review Terraform logs
- Contact the platform team

## Cleanup

To destroy the infrastructure:

```bash
terraform destroy
```

**Warning**: This will permanently delete all resources. Ensure backups are in place for critical data.

---

*Generated by TF Wizard - Enterprise Hub-and-Spoke Architecture*