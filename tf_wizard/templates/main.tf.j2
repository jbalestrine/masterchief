// Provider block (provider-specific helper)
{{ provider_block }}

// Resources
{% for r in resources %}
resource "{{ r.type }}" "{{ r.name }}" {
{% for k,v in r.args.items() %}
  {{ k }} = {{ v if v is string and v.startswith('"') else (v|tojson) }}
{% endfor %}
}
{% endfor %}
