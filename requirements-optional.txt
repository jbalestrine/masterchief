# =============================================================================
# MasterChief — Optional / Heavy Dependencies
# =============================================================================
# These are NOT needed to start the Flask app.
# Install only the groups you actually use.
#
# Usage:  pip install -r requirements-optional.txt
#   or install individual sections as needed.
# =============================================================================

# ---------------------------------------------------------------------------
# AI / ML  (local model inference, embeddings, image generation)
# NOTE: torch alone is ~2 GB. Install CPU-only build on machines without a GPU:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# ---------------------------------------------------------------------------
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
diffusers>=0.21.0
accelerate>=0.25.0
peft>=0.6.0
safetensors>=0.4.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
huggingface-hub>=0.19.0
tokenizers>=0.15.0
llama_cpp_python>=0.2.0
gguf>=0.1.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# ---------------------------------------------------------------------------
# Voice / Audio / TTS / STT
# Requires native system libraries (portaudio, ffmpeg, etc.)
# ---------------------------------------------------------------------------
pyttsx3>=2.90
openai-whisper>=20231117
sounddevice>=0.4.6
soundfile>=0.12.1
librosa>=0.10.0
pygame>=2.5.0
webrtcvad>=2.0.10
openwakeword>=0.5.0
TTS>=0.22.0

# ---------------------------------------------------------------------------
# Async / Message Queues / Streaming
# ---------------------------------------------------------------------------
aiokafka>=0.10.0
pika>=1.3.0
aiohttp>=3.9.0
eventlet>=0.33.0

# ---------------------------------------------------------------------------
# Additional Databases
# ---------------------------------------------------------------------------
redis>=5.0.0
psycopg2-binary>=2.9.0      # PostgreSQL
asyncpg>=0.29.0             # PostgreSQL async
aiomysql>=0.2.0             # MySQL async
aiosqlite>=0.19.0           # SQLite async
motor>=3.3.0                # MongoDB async
alembic>=1.13.0             # DB migrations
influxdb-client>=1.38.0     # Time-series DB

# ---------------------------------------------------------------------------
# Miscellaneous / Dev Tools
# ---------------------------------------------------------------------------
watchdog>=3.0.0
ollama>=0.1.0
croniter>=1.3.0
ruff>=0.1.0
statsd>=4.0.1
celery>=5.3.0
