Terraform Enterprise-ready Azure landing zone example (AKS + Monitoring)

To use:
  - Configure TFE credentials and create workspace `tfe-aks-demo` in organization `my-org`.
  - Push this repo to VCS and connect to the workspace, or use local Terraform CLI.

Generated at: examples\tfe_aks_monitoring
