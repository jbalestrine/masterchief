# 🚀 Quick Start - ONE FILE TO RUN IT ALL!

## Run Everything with One Command:

```bash
python run.py
```

That's it! This single file starts:
- ✅ Web GUI for data upload
- ✅ REST API
- ✅ All platform features

## What You Get:

### 🌙 Data Upload Web Interface
**Main Feature:** http://localhost:8080/api/v1/data/upload

Three full web pages:
1. **Upload** - Drag-and-drop file upload for training Echo
2. **Browse** - View and manage all uploaded files
3. **Manage** - Statistics and analytics

### 📤 Upload Training Data for Echo

**Supported Files:**
- Text: `.txt`, `.md`, `.log` 
- Data: `.json`, `.yaml`, `.csv`, `.xml`
- Audio: `.wav`, `.mp3`, `.ogg`, `.flac` (for voice training!)
- Archives: `.zip`, `.tar`, `.gz`

## Installation (First Time Only):

```bash
# Install dependencies
pip install -r requirements.txt

# Run the platform
python run.py
```

## Usage:

1. Run: `python run.py`
2. Open browser: `http://localhost:8080/api/v1/data/upload`
3. Drag and drop files to train Echo!

## Features:

✅ **Fully Integrated** - No configuration needed  
✅ **One Command** - `python run.py` does everything  
✅ **Web GUI** - Beautiful interface for all features  
✅ **Auto-setup** - Creates all directories automatically  
✅ **Echo Training** - Upload data for Echo to learn from  

## Troubleshooting:

**Problem:** Dependencies missing  
**Solution:** `pip install -r requirements.txt`

**Problem:** Port 8080 in use  
**Solution:** Edit `run.py` and change the port number

## Documentation:

- Full Web GUI Guide: `docs/WEB_GUI_INTEGRATION.md`
- API Reference: `docs/DATA_UPLOAD_API.md`
- Quick Start: `docs/UPLOAD_QUICKSTART.md`

---

**🌙 One file runs everything. That's the MasterChief way.**
