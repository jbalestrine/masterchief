# Staging Environment Configuration
# Inherits from dev

parent: "dev"

config:
  azure:
    resource_group: "rg-masterchief-staging"
    tags:
      environment: "staging"
  
  terraform:
    workspace: "staging"
    backend:
      resource_group: "rg-tfstate-staging"
      storage_account: "stmcstagingstate"
  
  deployment:
    auto_approve: false

secrets:
  azure_client_id: "$AZURE_CLIENT_ID_STAGING"
  azure_client_secret: "$AZURE_CLIENT_SECRET_STAGING"
