---
# Default variables for common role

common_packages:
  - vim
  - git
  - curl
  - wget
  - htop
  - net-tools
  - unzip
  - ca-certificates

system_timezone: "UTC"

configure_ntp: true
configure_firewall: true
enable_monitoring: false

# Example admin users structure
# admin_users:
#   - name: "admin1"
#     groups: ["sudo", "docker"]
#     shell: "/bin/bash"
#     ssh_key: "ssh-rsa AAAA..."
